import { apiClient } from ".";

export const apiMedia = async (title: string, creator: string, year:number, targetAudience:string, userrating: number, media_type: string) => {
try{
    let response = 
}
}