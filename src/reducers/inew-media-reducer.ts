import {INewMediaState} from ".";

const initalState: INewMediaState = {
    addFavMedia:''
}

export const newMediaReducer = (state = initalState, action:any) => {
    switch(action.type){
        case

        default:
            return state;
    }
}